#content.games

  = semantic_form_for @game do |f|

    = f.inputs do
      = f.input :name
      - f.input :hidden, as: :boolean

      = f.buttons do
        = f.commit_button
        %li= link_to 'Back', games_path
