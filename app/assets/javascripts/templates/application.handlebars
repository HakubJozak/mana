<div class='controls'>
  {{view Mana.BrowserView holder=current_player.hand }}
  {{view Mana.ChatView}}

  <div class="decks">
    <div class="status">
      Decks
    </div>

    <div class="piles">
      {{view Mana.PackView holder=current_player.deck }}
      <div class="pile-container">
        <button class="next-turn">
          Next  Turn
        </button>
      </div>
    </div>
  </div>
</div>
<div class='tables'>
  {{#each player in players}}
    {{view Mana.TableView player=player}}
  {{/each}}
</div>

{{outlet}}