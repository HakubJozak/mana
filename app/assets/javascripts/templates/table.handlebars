<div class="status">
  {{ player.name }}({{ player.id }})
</div>
<div class="slots">
  {{#each slot in player.slots}}
   {{view Mana.SlotView content=slot holder=slot position=_view.contentIndex player_id=player.id }}
  {{/each}}
</div>
